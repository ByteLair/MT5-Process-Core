# Cron example for maintenance
# Run every 5 minutes to ensure services are up
# Requires sudo for killing docker-proxy when needed

*/5 * * * * cd /home/felipe/MT5-Process-Core-full && /bin/bash scripts/maintenance.sh >> logs/maintenance.log 2>&1
