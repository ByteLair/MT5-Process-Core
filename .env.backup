# MT5 Backup - Environment Configuration Template

# Database
POSTGRES_DB=mt5_trading
POSTGRES_USER=trader
POSTGRES_PASSWORD=trader123
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Local Backup
BACKUP_DIR=/var/backups/mt5
KEEP_LOCAL_BACKUPS=3

# Remote Backup Server
# Exemplo: http://192.168.15.18:9101
BACKUP_API_URL=
BACKUP_API_TOKEN=

# Upload Settings
MAX_RETRIES=3
RETRY_DELAY=5# ============================================================================
# MT5 Trading DB - Backup Configuration
# ============================================================================

# Database Configuration
POSTGRES_DB=mt5_trading
POSTGRES_USER=trader
POSTGRES_PASSWORD=trader123
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Local Backup Settings
BACKUP_DIR=/var/backups/mt5
KEEP_LOCAL_BACKUPS=3


# Remote Backup Server (API)
BACKUP_API_URL=http://192.168.15.18:9101
BACKUP_API_TOKEN=mt5-backup-secure-token-2025

# Upload Settings
MAX_RETRIES=3
RETRY_DELAY=5

# ============================================================================
# INSTRUÇÕES DE USO:
# ============================================================================
# 1. Copie este arquivo para /etc/default/mt5-backup:
#    sudo cp .env.backup /etc/default/mt5-backup
#
# 2. Edite o arquivo e substitua SEU_IP_BACKUP pelo IP real:
#    sudo nano /etc/default/mt5-backup
#
# 3. Ajuste as permissões para proteger credenciais:
#    sudo chmod 600 /etc/default/mt5-backup
#    sudo chown root:root /etc/default/mt5-backup
#
# 4. O serviço systemd carregará automaticamente essas variáveis
# ============================================================================
